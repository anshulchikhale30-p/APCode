# tests

Reserved for cross-package and integration tests that do not belong to a
single internal package. Unit tests currently live next to the code they
cover (e.g. `internal/hardware/hardware_test.go`).
